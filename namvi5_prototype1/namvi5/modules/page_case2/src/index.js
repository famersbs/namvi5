angular.module('case1page', []).
  config(['$routeProvider', function($routeProvider) {
  
}]);

var pageController = function( $scope ){
	// View 전환 하는 방법을 찾아 보자 ~!
	$scope.target = target_URL;
	
};
